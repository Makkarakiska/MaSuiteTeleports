keep-request-alive: 20 #In seconds
spawn-type: server #You can use server, global and none
enable-first-spawn: false #Set true to enable first spawn
spawn-on-join: false #Set true to spawn player on join